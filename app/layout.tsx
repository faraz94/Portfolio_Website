import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "../components/ThemeProvider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Muhammad Faraz | Senior Frontend & Full Stack Engineer Portfolio",
  description: "Muhammad Faraz is a Senior Frontend and Full Stack Software Engineer specializing in building premium, high-performance web applications using React, Next.js, and TypeScript.",
  keywords: ["Muhammad Faraz", "Frontend Engineer", "Full Stack Engineer", "Next.js", "React", "TypeScript", "Kavak Portfolio"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <ThemeProvider>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
